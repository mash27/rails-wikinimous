<h1>Add a new article</h1>

<%= form_for(@article) do |a| %>
<%= a.label :title, "Add a article" %>
<%= a.text_field :title %>
<%= a.label :content, "Add content" %>
<%= a.text_area :content, cols: 80, rows:10 %>

<%= a.submit "Go" %>
<% end %>
